version: "3.9"
services:
    postgres:
        container_name: postgres
        image: postgres:latest
        volumes:
            - ./colorbrains_api/sql/:/docker-entrypoint-initdb.d/
            # - ./postgres-data:/var/lib/postgresql/data
        ports:
            - 5432:5432
        env_file:
            - ./.env
    colorbrains-api:
        restart: on-failure
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - 80:80
        env_file:
            - ./.env
        command: "/app/scripts/launch-and-populate-data.sh"
        entrypoint: ['bash', '-c']
